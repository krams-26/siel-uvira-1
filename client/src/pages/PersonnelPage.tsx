import DashboardLayout from "@/components/DashboardLayout";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Search, UserRound } from "lucide-react";
import { useMemo, useState } from "react";

export default function PersonnelPage() { const [search, setSearch] = useState(""); const users = trpc.administration.users.useQuery(); const assignments = trpc.administration.assignments.useQuery(); const filtered = useMemo(() => (users.data ?? []).filter(user => `${user.name ?? ""} ${user.email ?? ""} ${user.role}`.toLowerCase().includes(search.toLowerCase())), [users.data, search]); return <DashboardLayout><div className="siel-shell mx-auto min-h-[calc(100vh-2rem)] max-w-[1500px] rounded-2xl p-5 md:p-7"><div className="space-y-6"><div className="siel-section-bar flex items-center gap-4 rounded-xl px-6 py-5"><UserRound className="h-8 w-8" /><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-white/75">Personnel & affectations</p><h1 className="mt-1 text-3xl font-semibold">Gestion du personnel</h1></div></div><div className="grid gap-4 md:grid-cols-3"><Card className="border-0 shadow-sm"><CardContent className="p-5"><p className="text-sm text-muted-foreground">Agents enregistrés</p><p className="mt-2 text-3xl font-bold text-primary">{users.data?.length ?? 0}</p></CardContent></Card><Card className="border-0 shadow-sm"><CardContent className="p-5"><p className="text-sm text-muted-foreground">Affectations actives</p><p className="mt-2 text-3xl font-bold text-emerald-600">{assignments.data?.length ?? 0}</p></CardContent></Card><Card className="border-0 shadow-sm"><CardContent className="p-5"><p className="text-sm text-muted-foreground">Statut du registre</p><p className="mt-2 text-lg font-semibold">Traçabilité active</p></CardContent></Card></div><Card className="border-0 shadow-sm"><CardHeader className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div><CardTitle>Liste du personnel</CardTitle><p className="mt-1 text-sm text-muted-foreground">Fiches agents, rôles et rattachements institutionnels</p></div><div className="flex w-full items-center gap-2 rounded-lg border px-3 md:max-w-sm"><Search className="h-4 w-4 text-muted-foreground" /><Input value={search} onChange={event => setSearch(event.target.value)} className="border-0 shadow-none focus-visible:ring-0" placeholder="Rechercher un agent" /></div></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-muted-foreground"><th className="p-3">Nom complet</th><th className="p-3">Email</th><th className="p-3">Rôle</th><th className="p-3">Statut</th></tr></thead><tbody>{filtered.map(user => <tr key={user.id} className="border-b last:border-0"><td className="p-3 font-medium">{user.name ?? "Agent sans nom"}</td><td className="p-3 text-muted-foreground">{user.email ?? "—"}</td><td className="p-3"><Badge variant="outline">{user.role}</Badge></td><td className="p-3"><Badge variant={user.isActive ? "default" : "secondary"}>{user.isActive ? "Actif" : "Inactif"}</Badge></td></tr>)}</tbody></table>{!filtered.length && <p className="p-8 text-center text-sm text-muted-foreground">Aucun agent correspondant.</p>}</div></CardContent></Card></div></div></DashboardLayout>; }
